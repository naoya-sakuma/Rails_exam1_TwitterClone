<%= form_with(model: @mussitation, local: true) do |form| %>

  <div class="mussitation_content">
    <%= form.label :content %>
    <%= form.text_field :content %>
  </div>
  <%= form.submit :tweet%>
  <%= link_to '投稿一覧へ', mussitations_path %>
<% end %>
