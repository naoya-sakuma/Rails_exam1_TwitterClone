<h3>以下の内容で、送信する。</h3>
<%= form_with(model: @mussitation, local: true) do |form| %>
  <%= form.hidden_field :content %>
  <p>本文:<%= @mussitation.content %></p>
  <%= form.submit  "登録する" %>
  <%= link_to '戻る', :back %>
<% end %>
