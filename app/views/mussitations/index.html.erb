<h1>投稿一覧</h1>
<p><%= notice %></p>
<table>
  <tr>
    <th>投稿</th>
  </tr>
<% @mussitation.each do |mussitation| %>
  <tr>
    <td><%= mussitation.content %></td>
    <td><%= link_to "ブログを編集する", edit_mussitation_path(mussitation.id) %></td>
  </tr>
<% end %>
</table>
<%= link_to '新しくブログを投稿する', new_mussitation_path %>
